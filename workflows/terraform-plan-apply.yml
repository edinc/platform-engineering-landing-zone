stage: 06
status: executable
executable_location: .github/workflows/terraform-plan-apply.yml
description: Terraform plan and apply reusable workflow contract.
runner: Untrusted PR validation uses GitHub-hosted runners; Azure-backed plan/apply uses a private self-hosted runner by default.
