# Backstage plugins

Custom plugin development is out of scope for the MVP unless a later ADR changes
that direction.

Stage 09 uses community plugins only in the deployed portal. The
`cost-insights-azure/` package is an inactive fallback adapter scaffold for Azure
Cost Management if the community Cost Insights path regresses.
