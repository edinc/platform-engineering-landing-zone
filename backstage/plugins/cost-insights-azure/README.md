# Azure Cost Insights fallback adapter

This package contains the small Azure showback adapter helpers used by
the Backstage Cost Insights backend integration. The deployed MVP still uses the
community Cost Insights UI; platform-owned code only adapts cost showback CSV
showback data into the plugin API.
