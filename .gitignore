# macOS and editor state
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Python
.venv/
__pycache__/
*.py[cod]

# Node / Backstage
node_modules/
.pnpm-store/
dist/
coverage/

# Terraform
**/.terraform/
infrastructure/terraform/_modules/**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Local tool caches and generated credentials
.tools/
.cache/
*.kubeconfig
kubeconfig*
*.pem
*.key
*.pfx
*.p12

# Bootstrap / Terraform local inputs (never commit tenant-specific values)
backend.hcl
*.tfvars
*.auto.tfvars
*.auto.tfvars.json
*.tfvars.json
!*.tfvars.example
!templates/**/skeleton/**/*.tfvars
.env

# Brownfield discovery output (tenant inventory snapshots — never commit)
.discovery/
*.discovery/

# GitHub App manifest-flow credential output (never commit)
*github-app-creds*.json
backstage-github-app-creds.json
