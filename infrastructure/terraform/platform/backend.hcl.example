# Copy to backend.hcl (gitignored) and fill values from the _bootstrap stack outputs.
resource_group_name  = "rg-pe-bootstrap-sec"
storage_account_name = "stpebootsec0000"
container_name       = "platform"
key                  = "nonprod/platform.tfstate"
use_azuread_auth     = true
