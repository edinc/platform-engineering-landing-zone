# Example inputs for the platform shared services cluster-state repository stack.

github_owner          = "edinc"
repository_name       = "platform-cluster-state"
repository_visibility = "private"
default_branch        = "main"

codeowners = [
  "* @edinc/platform-engineering",
]

# Supply chain & CI/CD will add executable workflow checks and can then populate this list.
required_status_checks = []
