component_id   = "${{ values.componentId }}"
environment    = "${{ values.environment }}"
container_port = ${{ values.port }}
public_route   = ${{ values.publicRoute }}
scale_rule     = "${{ values.scaleRule }}"
