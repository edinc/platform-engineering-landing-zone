# Request egress exception template

Backstage template for time-boxed application egress exceptions. It
opens a PR containing a firewall allowlist patch proposal and a demo-profile
Cilium FQDN policy draft for platform review.
