# Onboard team template

Backstage meta-template for team onboarding. It creates a
`TeamOnboardingRequest` PR that the onboarding workflow applies with Terraform,
then the workflow opens reviewed namespace vending requests for the requested
environments.
