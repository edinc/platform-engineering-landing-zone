# Onboard team template

Backstage meta-template for Stage 10 team onboarding. It creates a
`TeamOnboardingRequest` PR that the Stage 10 workflow applies with Terraform,
then the workflow opens reviewed namespace vending requests for the requested
environments.
