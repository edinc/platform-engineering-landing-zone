# Policies

Policy assets are split by engine:

- `azure/` for Azure Policy definitions and initiatives.
- `rego/` for OPA/Rego policies tested with `conftest`.
- `kyverno/` for Kubernetes admission policies tested with `kyverno test`.
