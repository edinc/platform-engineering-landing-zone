apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: checkout-restricted
  namespace: team-payments
spec:
  interval: 5m
  storageNamespace: helm-team-payments
  serviceAccountName: helm-app
  chartRef:
    kind: OCIRepository
    name: checkout-signed
    namespace: team-payments
