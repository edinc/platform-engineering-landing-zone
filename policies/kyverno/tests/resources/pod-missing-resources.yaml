apiVersion: v1
kind: Pod
metadata:
  name: pod-missing-resources
  labels:
    app: sample
    team: platform-engineering
    costCenter: cc-platform
    dataClassification: internal
spec:
  securityContext:
    runAsNonRoot: true
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.10
      securityContext:
        privileged: false
        readOnlyRootFilesystem: true
