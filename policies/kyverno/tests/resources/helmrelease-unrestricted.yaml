apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: checkout-unrestricted
  namespace: team-payments
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: checkout-unsigned
    namespace: team-payments
